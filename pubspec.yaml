name: prawn_guard
description: PrawnGuard.ai — Enterprise Aquaculture Intelligence & Finance
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  fl_chart: ^0.66.0
  intl: ^0.19.0
  supabase_flutter: ^2.17.2
  shared_preferences: ^2.5.5
  http: ^1.6.0
  crypto: ^3.0.7
  flutter_riverpod: ^2.6.1
  go_router: ^17.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
